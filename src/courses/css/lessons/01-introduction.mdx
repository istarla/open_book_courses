# 01-introduction.mdx

<section id="what-is-css">
## What is CSS?

CSS (Cascading Style Sheets) is a stylesheet language used to describe the presentation of HTML documents.

```css
/* Basic CSS syntax */
selector {
  property: value;
}
```
</section>

<section id="css-history">
## Brief History of CSS

CSS was first proposed by Håkon Wium Lie in 1994 and became a W3C recommendation in 1996.

```css
/* CSS1 - Basic styling */
body {
  font-family: Arial, sans-serif;
  background-color: white;
}
```
</section>

<section id="css-syntax">
## Basic Syntax & Rules

CSS follows a simple syntax structure with selectors, properties, and values.

```css
/* CSS rule structure */
h1 {
  color: blue;
  font-size: 24px;
}
```
</section>

<section id="how-to-use">
## Ways to Add CSS

There are three main ways to add CSS to HTML: inline, internal, and external.

```html
<!-- Inline CSS -->
<p style="color: red;">This is red text</p>

<!-- Internal CSS -->
<style>
  p { color: blue; }
</style>

<!-- External CSS -->
<link rel="stylesheet" href="styles.css">
```
</section>

<section id="summary">
## Summary

CSS is essential for web styling and provides separation of content from presentation.
</section>
