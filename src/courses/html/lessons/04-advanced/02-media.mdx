# Media

HTML5 introduced native support for audio and video elements, eliminating the need for plugins like Flash. These elements provide a standardized way to embed multimedia content with built-in controls and extensive JavaScript APIs for custom functionality.

<section id="audio">

## Adding Audio

The `<audio>` element allows you to embed sound content directly into your web pages with cross-browser compatibility and accessibility features.

### Basic Audio Implementation

```html

<div class="video-gallery">
  <div class="video-item">
    <video width="300" height="200" controls poster="video1-thumb.jpg">
      <source src="tutorial-1.mp4" type="video/mp4">
    </video>
    <h4>HTML Basics Tutorial</h4>
    <p>Learn the fundamentals of HTML in this comprehensive tutorial.</p>
  </div>
  
  <div class="video-item">
    <video width="300" height="200" controls poster="video2-thumb.jpg">
      <source src="tutorial-2.mp4" type="video/mp4">
    </video>
    <h4>CSS Styling Guide</h4>
    <p>Master CSS techniques for beautiful web design.</p>
  </div>
  
  <div class="video-item">
    <video width="300" height="200" controls poster="video3-thumb.jpg">
      <source src="tutorial-3.mp4" type="video/mp4">
    </video>
    <h4>JavaScript Essentials</h4>
    <p>Interactive programming with JavaScript fundamentals.</p>
  </div>
</div>

<style>
.video-gallery {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 20px;
  margin: 20px 0;
}

.video-item {
  background: #f8f9fa;
  padding: 15px;
  border-radius: 10px;
  text-align: center;
}

.video-item h4 {
  margin: 10px 0 5px 0;
  color: #333;
}

.video-item p {
  color: #666;
  font-size: 14px;
  line-height: 1.4;
}
</style>
```

</section>

<section id="controls">

## Media Controls

Both audio and video elements come with built-in controls, but you can create custom interfaces for enhanced user experience and branding consistency.

### Custom Video Player

```html
<div class="custom-video-player">
  <video id="customVideo" width="640" height="360" poster="video-poster.jpg">
    <source src="sample-video.mp4" type="video/mp4">
    <source src="sample-video.webm" type="video/webm">
  </video>
  
  <div class="video-controls">
    <button id="videoPlayPause">▶️</button>
    <input type="range" id="videoProgress" value="0" max="100">
    <span id="videoCurrentTime">0:00</span>
    <span>/</span>
    <span id="videoDuration">0:00</span>
    <button id="muteButton">🔊</button>
    <input type="range" id="videoVolume" min="0" max="100" value="100">
    <button id="fullscreenButton">⛶</button>
  </div>
</div>

<script>
const video = document.getElementById('customVideo');
const playPauseBtn = document.getElementById('videoPlayPause');
const progressBar = document.getElementById('videoProgress');
const currentTimeSpan = document.getElementById('videoCurrentTime');
const durationSpan = document.getElementById('videoDuration');
const muteButton = document.getElementById('muteButton');
const volumeSlider = document.getElementById('videoVolume');
const fullscreenBtn = document.getElementById('fullscreenButton');

// Play/Pause functionality
playPauseBtn.addEventListener('click', () => {
  if (video.paused) {
    video.play();
    playPauseBtn.textContent = '⏸️';
  } else {
    video.pause();
    playPauseBtn.textContent = '▶️';
  }
});

// Update progress bar and time
video.addEventListener('timeupdate', () => {
  const progress = (video.currentTime / video.duration) * 100;
  progressBar.value = progress;
  currentTimeSpan.textContent = formatTime(video.currentTime);
});

// Seek functionality
progressBar.addEventListener('input', () => {
  const seekTime = (progressBar.value / 100) * video.duration;
  video.currentTime = seekTime;
});

// Mute/unmute functionality
muteButton.addEventListener('click', () => {
  if (video.muted) {
    video.muted = false;
    muteButton.textContent = '🔊';
  } else {
    video.muted = true;
    muteButton.textContent = '🔇';
  }
});

// Volume control
volumeSlider.addEventListener('input', () => {
  video.volume = volumeSlider.value / 100;
  if (video.volume === 0) {
    muteButton.textContent = '🔇';
  } else {
    muteButton.textContent = '🔊';
  }
});

// Fullscreen functionality
fullscreenBtn.addEventListener('click', () => {
  if (video.requestFullscreen) {
    video.requestFullscreen();
  } else if (video.webkitRequestFullscreen) {
    video.webkitRequestFullscreen();
  } else if (video.mozRequestFullScreen) {
    video.mozRequestFullScreen();
  }
});

// Display duration when metadata loads
video.addEventListener('loadedmetadata', () => {
  durationSpan.textContent = formatTime(video.duration);
});

// Reset play button when video ends
video.addEventListener('ended', () => {
  playPauseBtn.textContent = '▶️';
});

// Format time helper function
function formatTime(seconds) {
  const minutes = Math.floor(seconds / 60);
  const secs = Math.floor(seconds % 60);
  return `${minutes}:${secs.toString().padStart(2, '0')}`;
}
</script>

<style>
.custom-video-player {
  position: relative;
  display: inline-block;
  background: #000;
  border-radius: 10px;
  overflow: hidden;
}

.video-controls {
  display: flex;
  align-items: center;
  background: rgba(0, 0, 0, 0.8);
  color: white;
  padding: 10px;
  gap: 10px;
}

.video-controls button {
  background: transparent;
  border: none;
  color: white;
  cursor: pointer;
  padding: 5px;
  border-radius: 3px;
}

.video-controls button:hover {
  background: rgba(255, 255, 255, 0.1);
}

#videoProgress {
  flex-grow: 1;
  margin: 0 10px;
}

#videoVolume {
  width: 80px;
}
</style>
```

### Media Events and JavaScript API

```html
<div class="media-player-advanced">
  <video id="advancedVideo" width="600" height="338" controls>
    <source src="demo-video.mp4" type="video/mp4">
  </video>
  
  <div class="playback-controls">
    <button onclick="changePlaybackRate(0.5)">0.5x</button>
    <button onclick="changePlaybackRate(1)">1x</button>
    <button onclick="changePlaybackRate(1.25)">1.25x</button>
    <button onclick="changePlaybackRate(1.5)">1.5x</button>
    <button onclick="changePlaybackRate(2)">2x</button>
  </div>
  
  <div class="media-info" id="mediaInfo">
    <p>Status: <span id="videoStatus">Paused</span></p>
    <p>Current Time: <span id="currentTime">0:00</span></p>
    <p>Duration: <span id="duration">Loading...</span></p>
    <p>Playback Rate: <span id="playbackRate">1x</span></p>
    <p>Volume: <span id="volumeLevel">100%</span></p>
  </div>
</div>

<script>
const advancedVideo = document.getElementById('advancedVideo');

// Event listeners for video events
advancedVideo.addEventListener('loadstart', () => console.log('Started loading'));
advancedVideo.addEventListener('loadeddata', () => console.log('Data loaded'));
advancedVideo.addEventListener('canplay', () => console.log('Can start playing'));
advancedVideo.addEventListener('canplaythrough', () => console.log('Can play through'));

advancedVideo.addEventListener('play', () => {
  document.getElementById('videoStatus').textContent = 'Playing';
});

advancedVideo.addEventListener('pause', () => {
  document.getElementById('videoStatus').textContent = 'Paused';
});

advancedVideo.addEventListener('ended', () => {
  document.getElementById('videoStatus').textContent = 'Ended';
});

advancedVideo.addEventListener('timeupdate', () => {
  document.getElementById('currentTime').textContent = formatTime(advancedVideo.currentTime);
});

advancedVideo.addEventListener('loadedmetadata', () => {
  document.getElementById('duration').textContent = formatTime(advancedVideo.duration);
});

advancedVideo.addEventListener('volumechange', () => {
  const volumePercent = Math.round(advancedVideo.volume * 100);
  document.getElementById('volumeLevel').textContent = `${volumePercent}%`;
});

// Playback rate control
function changePlaybackRate(rate) {
  advancedVideo.playbackRate = rate;
  document.getElementById('playbackRate').textContent = `${rate}x`;
}

// Keyboard controls
document.addEventListener('keydown', (e) => {
  if (document.activeElement === advancedVideo || 
      document.activeElement === document.body) {
    switch(e.key) {
      case ' ':  // Spacebar
        e.preventDefault();
        if (advancedVideo.paused) {
          advancedVideo.play();
        } else {
          advancedVideo.pause();
        }
        break;
      case 'ArrowLeft':  // Left arrow - seek backward
        e.preventDefault();
        advancedVideo.currentTime -= 10;
        break;
      case 'ArrowRight':  // Right arrow - seek forward
        e.preventDefault();
        advancedVideo.currentTime += 10;
        break;
      case 'ArrowUp':  // Up arrow - volume up
        e.preventDefault();
        advancedVideo.volume = Math.min(1, advancedVideo.volume + 0.1);
        break;
      case 'ArrowDown':  // Down arrow - volume down
        e.preventDefault();
        advancedVideo.volume = Math.max(0, advancedVideo.volume - 0.1);
        break;
    }
  }
});

function formatTime(seconds) {
  const minutes = Math.floor(seconds / 60);
  const secs = Math.floor(seconds % 60);
  return `${minutes}:${secs.toString().padStart(2, '0')}`;
}
</script>

<style>
.media-player-advanced {
  max-width: 600px;
  margin: 20px 0;
}

.playback-controls {
  display: flex;
  gap: 10px;
  margin: 10px 0;
}

.playback-controls button {
  padding: 8px 16px;
  background: #007bff;
  color: white;
  border: none;
  border-radius: 4px;
  cursor: pointer;
}

.playback-controls button:hover {
  background: #0056b3;
}

.media-info {
  background: #f8f9fa;
  padding: 15px;
  border-radius: 8px;
  font-family: monospace;
}

.media-info p {
  margin: 5px 0;
}
</style>
```

### Accessibility Features

```html
<div class="accessible-media">
  <h3>Accessible Video Player</h3>
  
  <video id="accessibleVideo" 
         width="640" 
         height="360" 
         controls
         aria-label="Tutorial video about HTML media elements"
         poster="accessible-video-poster.jpg">
    <source src="tutorial-video.mp4" type="video/mp4">
    <source src="tutorial-video.webm" type="video/webm">
    
    <!-- Multiple subtitle tracks -->
    <track kind="subtitles" 
           src="subtitles-en.vtt" 
           srclang="en" 
           label="English subtitles" 
           default>
    <track kind="subtitles" 
           src="subtitles-es.vtt" 
           srclang="es" 
           label="Spanish subtitles">
    
    <!-- Audio descriptions for visually impaired users -->
    <track kind="descriptions" 
           src="audio-descriptions.vtt" 
           srclang="en" 
           label="Audio descriptions">
    
    <!-- Chapter navigation -->
    <track kind="chapters" 
           src="chapters.vtt" 
           srclang="en" 
           label="Chapters">
    
    <p>Your browser doesn't support HTML5 video. Here's a 
       <a href="tutorial-video.mp4">link to the video</a> instead.
    </p>
  </video>
  
  <div class="video-transcript">
    <h4>Video Transcript</h4>
    <details>
      <summary>Show transcript</summary>
      <div class="transcript-content">
        <p><strong>[0:00]</strong> Welcome to our HTML media tutorial.</p>
        <p><strong>[0:15]</strong> In this video, we'll learn about audio and video elements.</p>
        <p><strong>[0:30]</strong> First, let's look at the basic audio element...</p>
        <!-- Full transcript would continue here -->
      </div>
    </details>
  </div>
</div>

<style>
.accessible-media {
  max-width: 700px;
  margin: 20px 0;
}

.video-transcript {
  margin-top: 20px;
  background: #f8f9fa;
  padding: 15px;
  border-radius: 8px;
}

.transcript-content {
  max-height: 200px;
  overflow-y: auto;
  padding: 10px;
  background: white;
  border-radius: 4px;
  margin-top: 10px;
}

.transcript-content p {
  margin: 8px 0;
  line-height: 1.5;
}
</style>
```

</section>

HTML5 media elements provide powerful, accessible ways to include audio and video content in your web pages. With proper implementation of controls, captions, and fallbacks, you can create inclusive multimedia experiences that work across all browsers and devices.html
<audio controls>
  <source src="music.mp3" type="audio/mpeg" />
  <source src="music.ogg" type="audio/ogg" />
  <source src="music.wav" type="audio/wav" />
  Your browser does not support the audio element.
</audio>

```

### Audio with Different Formats

Different browsers support different audio formats, so it's best to provide multiple sources:

```html
<h3>Podcast Episode</h3>
<audio controls preload="metadata">
  <source src="podcast-episode-1.mp3" type="audio/mpeg">
  <source src="podcast-episode-1.ogg" type="audio/ogg">
  <p>Your browser doesn't support HTML5 audio. 
     <a href="podcast-episode-1.mp3">Download the audio file</a>.
  </p>
</audio>

<h3>Background Music</h3>
<audio controls loop>
  <source src="background-music.webm" type="audio/webm">
  <source src="background-music.mp3" type="audio/mpeg">
  <p>Audio not supported in your browser.</p>
</audio>
```

### Audio Attributes

Important attributes for audio elements:

```html
<!-- Autoplay audio (use carefully due to browser policies) -->
<audio controls autoplay muted>
  <source src="intro.mp3" type="audio/mpeg">
</audio>

<!-- Preload audio metadata -->
<audio controls preload="metadata">
  <source src="song.mp3" type="audio/mpeg">
</audio>

<!-- Loop audio continuously -->
<audio controls loop>
  <source src="ambient.mp3" type="audio/mpeg">
</audio>

<!-- Multiple audio files -->
<div class="audio-playlist">
  <h4>Track 1</h4>
  <audio controls>
    <source src="track1.mp3" type="audio/mpeg">
  </audio>
  
  <h4>Track 2</h4>
  <audio controls>
    <source src="track2.mp3" type="audio/mpeg">
  </audio>
  
  <h4>Track 3</h4>
  <audio controls>
    <source src="track3.mp3" type="audio/mpeg">
  </audio>
</div>
```

### Custom Audio Player

Create a custom audio player with JavaScript:

```html
<div class="custom-audio-player">
  <audio id="customAudio" preload="metadata">
    <source src="sample-audio.mp3" type="audio/mpeg">
  </audio>
  
  <div class="player-controls">
    <button id="playPauseBtn">▶️</button>
    <input type="range" id="progressBar" value="0" max="100">
    <span id="currentTime">0:00</span> / <span id="duration">0:00</span>
    <input type="range" id="volumeSlider" min="0" max="100" value="100">
  </div>
</div>

<script>
const audio = document.getElementById('customAudio');
const playPauseBtn = document.getElementById('playPauseBtn');
const progressBar = document.getElementById('progressBar');
const currentTimeSpan = document.getElementById('currentTime');
const durationSpan = document.getElementById('duration');
const volumeSlider = document.getElementById('volumeSlider');

// Play/Pause functionality
playPauseBtn.addEventListener('click', () => {
  if (audio.paused) {
    audio.play();
    playPauseBtn.textContent = '⏸️';
  } else {
    audio.pause();
    playPauseBtn.textContent = '▶️';
  }
});

// Update progress bar
audio.addEventListener('timeupdate', () => {
  const progress = (audio.currentTime / audio.duration) * 100;
  progressBar.value = progress;
  currentTimeSpan.textContent = formatTime(audio.currentTime);
});

// Seek functionality
progressBar.addEventListener('input', () => {
  const seekTime = (progressBar.value / 100) * audio.duration;
  audio.currentTime = seekTime;
});

// Volume control
volumeSlider.addEventListener('input', () => {
  audio.volume = volumeSlider.value / 100;
});

// Display duration when metadata loads
audio.addEventListener('loadedmetadata', () => {
  durationSpan.textContent = formatTime(audio.duration);
});

// Format time helper function
function formatTime(seconds) {
  const minutes = Math.floor(seconds / 60);
  const secs = Math.floor(seconds % 60);
  return `${minutes}:${secs.toString().padStart(2, '0')}`;
}
</script>

<style>
.custom-audio-player {
  background: #f8f9fa;
  padding: 20px;
  border-radius: 10px;
  max-width: 400px;
}

.player-controls {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-top: 10px;
}

#playPauseBtn {
  background: #007bff;
  color: white;
  border: none;
  padding: 8px 12px;
  border-radius: 5px;
  cursor: pointer;
}

#progressBar {
  flex-grow: 1;
}
</style>
```


<section id="video">

## Embedding Video

The `<video>` element provides native video playback capabilities with extensive control options and accessibility features.

### Basic Video Implementation

```html
<video width="640" height="360" controls>
  <source src="movie.mp4" type="video/mp4">
  <source src="movie.webm" type="video/webm">
  <source src="movie.ogg" type="video/ogg">
  Your browser does not support the video tag.
</video>
```

### Video with Poster and Attributes

```html
<!-- Video with poster image and various attributes -->
<video width="800" height="450" 
       controls 
       poster="video-thumbnail.jpg" 
       preload="metadata">
  <source src="presentation.mp4" type="video/mp4">
  <source src="presentation.webm" type="video/webm">
  <p>Your browser doesn't support HTML5 video. 
     <a href="presentation.mp4">Download the video</a>.
  </p>
</video>

<!-- Responsive video -->
<div class="video-container">
  <video controls poster="thumbnail.jpg">
    <source src="responsive-video.mp4" type="video/mp4">
    <source src="responsive-video.webm" type="video/webm">
  </video>
</div>

<style>
.video-container {
  position: relative;
  width: 100%;
  max-width: 800px;
  height: 0;
  padding-bottom: 56.25%; /* 16:9 aspect ratio */
}

.video-container video {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
}
</style>
```

### Video with Subtitles and Captions

```html
<video width="720" height="405" controls>
  <source src="movie-with-subtitles.mp4" type="video/mp4">
  <source src="movie-with-subtitles.webm" type="video/webm">
  
  <!-- Subtitles in different languages -->
  <track kind="subtitles" src="subtitles-en.vtt" srclang="en" label="English" default>
  <track kind="subtitles" src="subtitles-es.vtt" srclang="es" label="Spanish">
  <track kind="subtitles" src="subtitles-fr.vtt" srclang="fr" label="French">
  
  <!-- Captions for accessibility -->
  <track kind="captions" src="captions-en.vtt" srclang="en" label="English Captions">
  
  <!-- Chapter markers -->
  <track kind="chapters" src="chapters.vtt" srclang="en" label="Chapters">
  
  Your browser does not support the video element.
</video>
```

### Background Video

```html
<div class="hero-section">
  <video class="background-video" autoplay muted loop>
    <source src="hero-background.mp4" type="video/mp4">
    <source src="hero-background.webm" type="video/webm">
  </video>
  
  <div class="hero-content">
    <h1>Welcome to Our Website</h1>
    <p>Experience the difference with our amazing services</p>
    <button class="cta-button">Get Started</button>
  </div>
</div>

<style>
.hero-section {
  position: relative;
  height: 100vh;
  overflow: hidden;
  display: flex;
  align-items: center;
  justify-content: center;
}

.background-video {
  position: absolute;
  top: 50%;
  left: 50%;
  min-width: 100%;
  min-height: 100%;
  width: auto;
  height: auto;
  transform: translateX(-50%) translateY(-50%);
  z-index: -1;
}

.hero-content {
  text-align: center;
  color: white;
  z-index: 1;
  background: rgba(0, 0, 0, 0.5);
  padding: 40px;
  border-radius: 10px;
}

.cta-button {
  background: #007bff;
  color: white;
  padding: 12px 24px;
  border: none;
  border-radius: 5px;
  font-size: 18px;
  cursor: pointer;
  margin-top: 20px;
}
</style>
```
</section>